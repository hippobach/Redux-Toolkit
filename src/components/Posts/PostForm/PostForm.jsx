import classNames from 'classnames/bind';
import styles from './PostForm.module.scss';

const cx = classNames.bind(styles);

const PostForm = () => {
    return <div className={cx('wrapper')}>PostForm</div>;
};

export default PostForm;
